name: NATS Bridge

on:
  pull_request:
    paths:
      - 'NATS_bridge/**'
      - .github/workflows/build_nats-bridge.yml
  push:
    branches:
      - main
    paths:
      - 'NATS_bridge/**'
      - .github/workflows/build_nats-bridge.yml
  schedule:
    - cron: "0 2 1 * *"

jobs:
  fieldai-nats-bridge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
          context: NATS_bridge/
          push: false
          tags: fieldai-nats-bridge:latest
